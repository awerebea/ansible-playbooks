---
all:
  vars:
    username_access_pass: user
    userpass_access_pass: '{{ lookup("env", "ANSIBLE_PASS_UBUNTU_VM") }}'
    username_access_key: admin
    userpass_access_key: adminpass
    ssh_keys_dir: ~/.ssh/ubuntu-vm

  children:
    servers:
      hosts:
        server-1:
          ansible_host: 192.168.100.5
          ansible_port: 22
