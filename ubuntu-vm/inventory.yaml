---
all:
  vars:
    user_passbase: user
    pass_passbase: '{{ lookup("env", "ANSIBLE_PASS_UBUNTU_VM") }}'
    user_keybase: admin
    userpass_access_key: adminpass
    ssh_keys_dir: ~/.ssh/ubuntu-vm

  children:
    servers:
      hosts:
        server-1:
          ansible_host: 192.168.100.14
          ansible_port: 22
