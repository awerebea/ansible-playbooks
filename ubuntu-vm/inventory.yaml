---
all:
  vars:
    username_key_access: admin
    userpass_key_access: adminpass
    username_pass_access: user
    userpass_pass_access: '{{ lookup("env", "ANSIBLE_PASS_UBUNTU_VM") }}'
    ssh_keypairs_dir: ~/.ssh/ubuntu-vm


  children:
    servers:
      hosts:
        server-1:
          ansible_host: 192.168.100.5
          ansible_port: 22
