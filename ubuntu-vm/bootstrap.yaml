---
- name: Bootstrap servers
  hosts: all
  gather_facts: false

  roles:
    - role: remote-user-key-based-access
      vars:
        - ansible_user: '{{ username_access_pass }}'
        - ansible_password: '{{ userpass_access_pass }}'
        - ansible_become_pass: '{{ userpass_access_pass }}'
